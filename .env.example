# DeclareLang Environment Variables
# Copy this file to .env and fill in your values

# Filesystem MCP Server - Project root directory
ALLOWED_DIRECTORIES=/path/to/declarelang

# GitHub Personal Access Token for MCP GitHub server
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user
GITHUB_TOKEN=ghp_your_token_here

# PostgreSQL connection string for MCP server
# Format: postgresql://[user[:password]@][host][:port]/database
# Default local setup (no password):
POSTGRES_CONNECTION_STRING=postgresql://localhost/declarelang_dev
# With credentials: postgresql://username:password@localhost:5432/declarelang_dev
# Docker: postgresql://postgres:password@localhost:5432/declarelang_dev

# Optional: Sentry (if added later)
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
